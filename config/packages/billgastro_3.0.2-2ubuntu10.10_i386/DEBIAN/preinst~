#!/bin/bash -e

. /usr/share/debconf/confmodule

case $1 in
  install)
    echo "Removing default Apache site..."
    rm -f /etc/apache2/sites-enabled/000-default
  ;;
  upgrade)
    #do something
  ;;
  *)
    echo "preinst called with unknown argument '$1'" 1>&2
    exit 1
  ;;
esac
