%h2= t('activerecord.models.page.other')

= link_to (t :new), new_page_path, :class => 'links'

= link_to '>', "/pages/forward/#{ @page.id }", :remote => true
#page= render :partial => 'page', :locals => { :page => @page, :partial_htmls => @partial_htmls }
