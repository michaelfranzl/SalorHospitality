$('#on_preparation_list').html('<%= escape_javascript render :partial => 'preparation', :collection => @on_list, :locals => { :hidden => false } %>');

$('#to_preparation_list').html('<%= escape_javascript render :partial => 'preparation', :collection => @to_list, :locals => { :hidden => true } %>');

$('#to_preparation_list .preparation').slideDown();
