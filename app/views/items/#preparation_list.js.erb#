$('#on_preparation_list').html('<%= escape_javascript render :partial => 'item_notification', :collection => @on_list, :locals => { :hidden => false } %>');

$('#to_preparation_list').html('<%= escape_javascript render :partial => 'item_notification', :collection => @to_list, :locals => { :hidden => true } %>');

$('#to_preparation_list .item_notification').slideDown();
