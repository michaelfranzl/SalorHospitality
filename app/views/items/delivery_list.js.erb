$('#on_delivery_list').html('<%= escape_javascript render :partial => 'delivery', :collection => @on_list, :locals => { :hidden => false } %>');

$('#to_delivery_list').html('<%= escape_javascript render :partial => 'delivery', :collection => @to_list, :locals => { :hidden => true } %>');

$('#to_delivery_list .delivery').slideDown();
