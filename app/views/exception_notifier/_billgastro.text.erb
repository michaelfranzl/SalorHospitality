BillGastro Version <%= File.open('VERSION', 'r').read %>
User: <%= @request.session[:user_id].login if @request.session[:user_id]  %>

Company: <%= @request.session[:user_id].company.name if @request.session[:user_id]  %>
