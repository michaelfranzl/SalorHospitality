#page.visual_effect :highlight, "invoice_#{@order.order.id}"
page.replace_html 'invoices', render(:partial => 'invoice_form', :collection => @orders )
page.visual_effect :highlight, "order_#{ @order.id }_item_#{ @item_to_split.id }" if not @item_to_split.count.zero?
