page.replace_html 'tables', render(@tables)

if workstation?
  if session[:admin_interface] == false
    page << 'tableupdates = 2;'
    page << "Effect.SlideUp('admin');"
  else
    page << 'tableupdates = -1;'
    page << "Effect.SlideDown('admin');"
  end
end
