- if ipod?
  %img#logo{ :src => "/images/logo-small.png", :width => 200, :height => 77 }

= form_tag url_for(:controller => :orders, :action => :login), :remote => true do
  %p
    = label_tag 'login', (t '.username')
    %select{ :name => 'login' }
      - @users.each do |u|
        %option{ :value => u.login }== #{ u.login } (#{ MyGlobals::language_hash[u.language.to_s] })
  %p
    = label_tag 'password', (t '.password')
    = password_field_tag 'password', '', :size => 9
  %p
    = submit_tag t('.login'), :class => 'submit'
