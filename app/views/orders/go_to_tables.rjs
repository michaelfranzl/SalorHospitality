page.hide 'orderform', 'invoiceform'
page.replace_html 'tables', render(@tables)
page.show 'tables', 'rooms'
page.replace_html 'order_info', t(:loading)
if ipod?
  page << "tableupdates = true;"
else
  page << "tableupdates = #{ (session[:admin_interface] == true) ? 'false' : 'true' };"
end
