$('#order_<%= @item.order.id %>_item_<%= @item.id %>').css('backgroundColor', '<%= @current_vendor.taxes.find_by_id(@item.taxes.keys.first).color %>');
$('#order_<%= @item.order.id %>_item_<%= @item.id %> .count').html('<%= @current_vendor.taxes.find_by_id(@item.taxes.keys.first).letter %>');
