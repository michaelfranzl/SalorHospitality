if (invoice_update == true) {
  route('invoice');
  $('#invoices').html('<%= escape_javascript render :partial => 'orders/invoice_form', :collection => @orders %>');
}
submit_json.split_items_hash.original = <%= @orders.first ? @orders.first.id : 'null' %>;
submit_json.split_items_hash.partner = <%= @orders.last ? @orders.last.id : 'null' %>;